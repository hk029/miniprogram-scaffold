# piggy-dividend-bank

Taro + Go 小程序。

Layout: `header-tabbar` — HeaderTabBarLayout

## Start

```bash
cd miniprogram && npm install && npm run dev:weapp
cd server && go mod tidy && go run main.go
```
